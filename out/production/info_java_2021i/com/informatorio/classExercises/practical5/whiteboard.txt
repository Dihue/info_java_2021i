Resolución de problemas (pasos):

    >   Servicio Reader (ver ejemplos propuesto nº3)
    >   Necesitas una Clase Empleado (con los datos a guardar)
    >   Cada línea es un empleado que se almacena en una lista
    >   Crear un método que trate a cada línea (String), donde
        separe los campos y crear el Objeto Empleado
    >   Otro método que convierta la fecha String en tipo Fecha
        (LocalDate), también una Clase Date Formatter para con-
        vertir String en LocalDate
    >   Otro método que convierte el String (sueldo) en tipos
        double/Double, float, BigDecimal

Cálculos (sobre la lista final)

    >   Método que devuelva todos los empleados que comienzan con
        una letra dada en el apellido
    >   Método que devuelva el empleado más jóven y más viejo (se
        necesita función para calcular edad según fecha)
    >   Método que devuelva que empleado gana más y cual menos
    >   Opcional: imprimir todos los empleado por orden alfabetico
        (nombre y apellido)